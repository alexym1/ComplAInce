name: 'Check code quality'

runs:
  using: 'composite'
  steps:
    - name: checks rd code quality
      shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install pre-commit
        make install-prod
        pre-commit run --config "config/pre-commit/.pre-commit-config.yaml" --all-files
